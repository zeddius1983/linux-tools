Select FastFlowLM release
Choose which FastFlowLM release to install:
setup,build
arg|FLM_REF
items-cmd|curl -fsSL 'https://api.github.com/repos/FastFlowLM/FastFlowLM/releases?per_page=10' | grep '"tag_name"' | sed 's/.*: "//;s/",*$//'
